[build]
  publish = "dist"
  functions = "functions"

[dev] # 이 부분은 로컬 개발용이지만, 있어도 배포에 문제는 없습니다.
  publish = "dist"
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200